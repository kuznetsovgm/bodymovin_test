# Что нового: Мониторинг и Логирование

## 🎉 Добавлен полный стек мониторинга!

Ваш бот теперь включает профессиональный мониторинг с использованием Prometheus, Loki и Grafana.

## 🚀 Быстрый старт

### 1. Обновите `.env` файл

Добавьте новые переменные (или скопируйте из `.env.example`):

```bash
# Logging Configuration
LOG_LEVEL=info
ENABLE_LOKI=true
LOKI_HOST=http://loki:3100

# Metrics Configuration
METRICS_PORT=9090

# Grafana Configuration (опционально)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_password
```

### 2. Перезапустите сервисы

```bash
# Остановите текущие контейнеры
docker-compose down

# Запустите обновленный стек
docker-compose up -d

# Проверьте статус
docker-compose ps
```

### 3. Откройте Grafana

Перейдите по адресу: http://localhost:3000

- **Логин:** admin
- **Пароль:** admin (или из вашего `.env`)

Выберите дашборд "Bodymovin Bot Overview" для просмотра метрик.

## 📊 Что вы получаете

### Метрики в реальном времени:

- ⚡ Скорость обработки inline-запросов
- 🎨 Время генерации стикеров по типам анимации
- 💾 Эффективность кэша (hit/miss rate)
- 🔴 Мониторинг ошибок
- 🔗 Статус подключения к Redis
- 💻 Использование CPU и памяти

### Централизованное логирование:

- 📝 Все логи в одном месте (Loki)
- 🔍 Поиск по логам в Grafana
- 📊 Структурированные события (JSON)

### Визуализация:

- 📈 Готовый дашборд в Grafana
- 🎯 Prometheus для запросов метрик
- 🔔 Готовые правила алертов (опционально)

## 🔗 Полезные ссылки

После запуска доступны:

- **Grafana Dashboard:** http://localhost:3000
- **Prometheus:** http://localhost:9091
- **Bot Metrics:** http://localhost:9095/metrics
- **Bot Health:** http://localhost:9095/health

## 📚 Документация

- [MONITORING.md](./MONITORING.md) - Полное руководство по мониторингу
- [QUICKSTART.md](./QUICKSTART.md) - Быстрый старт
- [MONITORING_SUMMARY.md](./MONITORING_SUMMARY.md) - Описание изменений

## 🛠️ Проверка здоровья

Запустите скрипт для проверки всех сервисов:

```bash
./scripts/health-check.sh
```

## ⚙️ Что изменилось в коде

### Новые файлы:

- `src/metrics.ts` - Prometheus метрики
- `src/logger.ts` - Структурированное логирование
- `prometheus.yml` - Конфигурация Prometheus
- `loki-config.yml` - Конфигурация Loki
- `grafana/provisioning/` - Автоконфигурация Grafana

### Обновленные файлы:

- `src/bot.ts` - Добавлены метрики и логирование
- `docker-compose.yml` - Добавлены сервисы мониторинга
- `Dockerfile` - Добавлен health check
- `package.json` - Новые зависимости

## 📦 Новые зависимости

Автоматически установлены:

- `prom-client` - Prometheus клиент
- `winston` - Логирование
- `winston-loki` - Отправка логов в Loki

## 🎯 Примеры использования

### Просмотр метрик в Prometheus:

```promql
# Скорость запросов
rate(bot_inline_queries_total[5m])

# 95-й перцентиль времени генерации
histogram_quantile(0.95, rate(bot_sticker_generation_duration_seconds_bucket[5m]))

# Эффективность кэша
rate(bot_cache_hits_total[5m]) / (rate(bot_cache_hits_total[5m]) + rate(bot_cache_misses_total[5m]))
```

### Поиск в логах (Loki):

```logql
# Все логи
{app="bodymovin-bot"}

# Только ошибки
{app="bodymovin-bot"} |= "error"

# События генерации
{app="bodymovin-bot"} | json | event="sticker_generation"
```

## 🚨 Troubleshooting

### Метрики не отображаются?

```bash
# Проверьте endpoint метрик
curl http://localhost:9090/metrics

# Проверьте targets в Prometheus
# Откройте: http://localhost:9091/targets
```

### Логи не появляются в Loki?

Убедитесь, что `ENABLE_LOKI=true` в `.env` и перезапустите:

```bash
docker-compose restart bot
```

### Grafana не показывает данные?

1. Проверьте Data Sources в Grafana (Settings → Data Sources)
2. Убедитесь, что Prometheus и Loki доступны
3. Попробуйте выполнить запрос в Explore

## 💡 Советы

1. **В production** используйте `LOG_LEVEL=info` для оптимальной производительности
2. **В development** используйте `LOG_LEVEL=debug` для детальной отладки
3. **Регулярно проверяйте дашборд** для раннего обнаружения проблем
4. **Экспортируйте дашборды** Grafana для backup

## 🔄 Откат изменений

Если вы хотите вернуться к старой версии без мониторинга:

1. Используйте старый `docker-compose.yml`
2. Удалите импорты metrics и logger из `src/bot.ts`
3. Удалите зависимости: `npm uninstall prom-client winston winston-loki`

Но зачем? Мониторинг - это круто! 😎

## 🎓 Дополнительные ресурсы

- [Prometheus Documentation](https://prometheus.io/docs/)
- [Grafana Loki Documentation](https://grafana.com/docs/loki/latest/)
- [Grafana Documentation](https://grafana.com/docs/grafana/latest/)

---

**Вопросы?** Читайте [MONITORING.md](./MONITORING.md) или [QUICKSTART.md](./QUICKSTART.md)
